import React from 'react';
import { movies } from '../data';

const Movies = (movies) => {
  debugger
  return (
    <div>
      <h1>Movies Page</h1>
      {this.movieMapper}
    </div>
  );
};

export default Movies;
