/*Could not use function because test is doing a shollow mount.*/
/*import React from "react";

const DisplayData = ({ collection, className = "" }) => {
  return collection.map((element) => (
    <div className={className} key={element.name}>
      <h3>{element.name}</h3>
      <ul>
        {element.movies.map((movie) => (
          <li>{movie}</li>
        ))}
      </ul>
    </div>
  ));
};

export default DisplayData;*/
