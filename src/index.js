import React from 'react';
import { render } from 'react-dom';
import App from './containers/App';

// This is cool and all, but I've done a couple of similar exercises just recently, so I'm not really feeling it.

render (
  <App />,
  document.getElementById('root')
);
